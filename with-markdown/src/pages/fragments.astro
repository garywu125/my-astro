---
const name = "Component"
const items = ["Dog", "Cat", "Platipus"];
---

<h1>Hello {name}</h1>
<ul>
  {items.map((item) => (
    <li>{item}</li>
  ))}
</ul>

<h1>Hello {name}</h1>
<ul>
  {items.map((item) => (
    <div>
      <li>Red {item}</li>
      <li>Blue {item}</li>
      <li>Green {item}</li>
    </div>        
  ))}
</ul>

<ul>
  {items.map((item) => (
    <>   
      <li>Red {item}</li>
      <li>Blue {item}</li>
      <li>Green {item}</li>
    </>
  ))}
</ul>


<ul>
  {items.map((item) => (
    <fragement>
      <li>Red {item}</li>
      <li>Blue {item}</li>
      <li>Green {item}</li>
    </fragement>   
  ))}
</ul>