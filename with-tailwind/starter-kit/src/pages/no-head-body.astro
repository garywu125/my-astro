<!-- no frontmatter , no head/body -->

<!-- astro component template only html & script -->
<p id="demo">hello astro , keep your eys to the sky</p>
<button onclick="myFunction()">press me</button>
<button onclick="myAlert()">Sweet Alert 2</button>
<!-- client script , write script && load from cdn-->

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>   
<script  language="javascript">
// using client fetch
 getData() 
// client fetch can be use in preact component
  async function getData(){
    const response = await fetch('https://jsonplaceholder.typicode.com/todos/1')
    const data = await response.json()
    console.log(data) 
  }


//   {
// 	"userId": 1,
// 	"id": 1,
// 	"title": "delectus aut autem",
// 	"completed": false
// }

 const myAlert=()=>{
          Swal.fire({
            title: 'Do you want to save the changes?',
            showDenyButton: true,
            showCancelButton: true,
            confirmButtonText: `Save`,
            denyButtonText: `Don't save`,
          }).then((result) => {
            /* Read more about isConfirmed, isDenied below */
            if (result.isConfirmed) {
              Swal.fire('Saved!', '', 'success')
            } else if (result.isDenied) {
              Swal.fire('Changes are not saved', '', 'info')
            }
          })
      }
  document.getElementById("demo").innerText="content changed by script"  

  function myFunction() {
    document.getElementById("demo").innerHTML = "hello astro , keep your eys to the sky";  
}
</script>